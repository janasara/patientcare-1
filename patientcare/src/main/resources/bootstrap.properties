# Place for common configurations across environments
server.port: 9090

# Eureke Registry
spring.application.name=diagnostic-centers
eureka.client.service-url.defaultZone=http://localhost:8761/eureka

# Config client to take advantage of Discovery server
#spring.cloud.config.discovery.enabled=true
# If there is change in the config server name
#spring.cloud.config.discovery.serviceId=pc-config-server
spring.cloud.config.uri=http://localhost:9999
#spring.cloud.config.server.git.timeout=10000

spring.profiles.active=dev
management.security.enabled=false

